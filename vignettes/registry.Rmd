---
title: "Registry"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Registry}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, setup, include = FALSE}
library('magrittr')
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

The following traders have registered and are currently setting up their IBKR
paper trader accounts:
```{r, echo = FALSE}
DT::datatable(
  fintech.trading.competition:::registry,
  extensions = 'Scroller', 
  rownames   = FALSE,
  options = list(
    columnDefs = list(list(className = 'dt-center', targets = '_all')),
    deferRender = TRUE,
    ordering    = FALSE,
    scrollY     = 500,
    scroller    = TRUE
  )
)
```
